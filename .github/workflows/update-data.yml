name: Update static DB (YouTube)

on:
  push:
    branches: [main, pre-production, production]
    paths:
      - "scripts/update-data.js"
      - "docs/data/**"
  schedule:
    # Every Monday 06:00 UTC -> refresh YouTube videos
    - cron: "0 6 * * 1"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Update data
        run: node scripts/update-data.js

      - name: Commit if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git status --porcelain
          if [ -n "$(git status --porcelain docs/data)" ]; then
            git add docs/data/youtube.json docs/data/db.json
            git commit -m "chore(data): update youtube db [skip ci]"
            git push
          else
            echo "No changes in docs/data"
          fi
